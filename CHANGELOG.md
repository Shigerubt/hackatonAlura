# Changelog

## v0.1.0
- Merge PR #5: `feature-prueba` → `main` (churn stats + dashboard improvements)
- Merge PR #6: `feature-auth-integration` → `main` (JWT/token extraction robustness, `roles`/`rol` claim compatibility, secret fallback)
- Docker Compose and healthchecks validated; dashboard token normalization and CSV batch fixes.

## Upcoming
- CI required checks (`maven-tests`, `python-ds`, `python-dashboard`).
- Sequential merging of remaining branches (auto-delete after merge).
